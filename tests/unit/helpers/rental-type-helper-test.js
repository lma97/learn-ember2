import { rentalTypeHelper } from 'super-rentals/helpers/rental-type-helper';
import { module, test } from 'qunit';

module('Unit | Helper | rental type helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalTypeHelper([42]);
  assert.ok(result);
});
